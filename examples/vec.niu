struct Vec<T> { t: T }

impl<T> Vec<T> {
  fn new(t: T) -> Self { Vec { t: t } }
  fn throw(v: Self) -> Self { v }
}

fn apply<T>(v: Vec<T>) -> Vec<T> {
  v.throw()
}

fn main() -> i64 {
  apply(Vec<i64>::new(1i64));
  0i64
}
