struct Vec<T> { t: T }

impl<T> Vec<T> {
  fn new() -> Self $${std::vector<$ty(T)>()}$$
  fn push_back(self: Self, t: T) -> bool $${$arg(self).push_back($arg(t))}$$
}

fn main() -> Vec<i64> {
  let vec = Vec<i64>::new();
  vec.push_back(1i64);
  vec
}
